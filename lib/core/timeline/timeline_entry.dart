class TimelineEntry {
  final String title;
  final String description;
  final DateTime timestamp;

  const TimelineEntry({
    required this.title,
    required this.description,
    required this.timestamp,
  });
}